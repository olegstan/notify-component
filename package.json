{
  "name": "notify-component",
  "productName": "notify-component",
  "version": "1.1.9",
  "description": "notify-component",
  "main": "dist/index.js",
  "keywords": [],
  "author": {
    "name": "Abramov Oleg",
    "email": "olegstan1@gmail.com"
  },
  "scripts": {
    "build:map": "babel src -d dist  --source-maps && copyfiles -u 1 src/images/* dist/",
    "build": "babel src -d dist && copyfiles -u 1 src/images/* dist/"
  },
  "license": "MIT",

  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.14.1",
    "styled-components": "^5.3.5"
  },
  "devDependencies": {
    "@babel/preset-react": "^7.0.0",
    "copyfiles": "^2.4.1"
  },
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not op_mini all"
  ]
}
